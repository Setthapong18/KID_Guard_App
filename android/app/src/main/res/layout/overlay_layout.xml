<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/overlay_gradient_bg"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/rounded_dialog_bg"
        android:orientation="vertical"
        android:padding="40dp"
        android:elevation="24dp">

        <!-- Icon with glow effect -->
        <FrameLayout
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_gravity="center_horizontal">

            <View
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:background="@drawable/icon_glow_bg"/>

            <ImageView
                android:id="@+id/overlay_icon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="center"
                android:src="@android:drawable/ic_secure"
                android:tint="#EEFFFFFF"
                android:contentDescription="Lock Icon" />
        </FrameLayout>

        <!-- Title -->
        <TextView
            android:id="@+id/overlay_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="28dp"
            android:text="แอพนี้ถูกล็อค"
            android:textColor="#F2FFFFFF"
            android:textSize="22sp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="-0.01" />

        <!-- Message -->
        <TextView
            android:id="@+id/overlay_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="12dp"
            android:text="ไม่สามารถเข้าถึงแอพนี้ได้ในขณะนี้"
            android:textColor="#80FFFFFF"
            android:textSize="15sp"
            android:gravity="center"
            android:lineSpacingMultiplier="1.3"
            android:fontFamily="sans-serif" />

        <!-- Glass Button -->
        <Button
            android:id="@+id/overlay_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="36dp"
            android:background="@drawable/glass_button_bg"
            android:text="รับทราบ"
            android:textColor="#F0FFFFFF"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"
            android:paddingVertical="16dp"
            android:stateListAnimator="@null"
            android:elevation="0dp" />

    </LinearLayout>
</FrameLayout>
